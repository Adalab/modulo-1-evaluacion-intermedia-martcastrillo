# Repositorio de ejercicios de evaluación intermedia

Consta de el archivo principal (index.html) y una carpeta donde se pueden encontrar la hoja de reset (reset.css) y la hoja de estilos propia del proyecto (main.css) p

Por tratarse de un proyecto de un tamaño no muy grande se incluyen directamente las media queries para tamaños de pantalla mayor que 768px en el la hoja de estilos principal.
